# Nim compiler cheatshet
##compile and run
nim compile --run greetings.nim
nim compile --run greetings.nim arg1 arg2
nim c -r greetings.nim

## compile release version
nim c -d:release greetings.nim


